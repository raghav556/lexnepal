/**
 * Transitional Convex boundary.
 *
 * Legacy screens may import this module while they are moved to domain hooks.
 * Components must never import `convex/react` directly. Delete this bridge after
 * the last Convex-backed domain is cut over.
 */
export * from "convex/react";
